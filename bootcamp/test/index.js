/*
 * Create a function called 'shuffle'.
 * This function will take an array and return a new array with its
 * elements in a different order than the original array.
 */

var shuffle = function(){
  return ['a'];
};