/* Populate the colors array provided to you below */
var colors = [];

///////////////////////////////

const main = function(){ // Do not remove this function
  /* Log your colors below by dynamically
  accessing them from the colors array */

  // hint: console.log(colors[index]);

};
