/* global names _*/

// solution code here:
// make sure it passes all of the test specs
const names = _.shuffle(['Max', 'John', 'Harvey', 'Maggie', 'Allison']);

var firstElement = names[0];
var lastElement = names[names.length - 1];
