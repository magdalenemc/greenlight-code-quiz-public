/* global names _*/

/*
You have a global array called names which currently
contains 6 names. This array has already been created
for you.

1.) Create a variable called firstElement and assign it
  to the first item in myArray
2.) Create a variable called lasElement and assign it
  dynamically to the last item in myArray;

*/
const names = _.shuffle(['Max', 'John', 'Harvey', 'Maggie', 'Allison']);

// Your code below
